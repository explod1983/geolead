Rules to check before any task:

1) Do NOT push to any remote unless the user explicitly says so for that specific push.
2) Confirm actions when unsure; ask before proceeding if instructions are ambiguous.
3) Avoid changing .env or secrets unless explicitly requested.
